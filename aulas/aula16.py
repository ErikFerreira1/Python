#Tuplas
#lanche = ('Hamburguer', 'Suco', 'Pizza', 'Pudim')
#for cont in range (0, len(lanche)):
    #print(lanche[cont])
#Tuplas são imutaveis
#for comida in lanche:
   # print(f'Eu vou comer {comida}')
#for pos, comida in enumerate(lanche):
   # print(f'Eu vou comer {comida}, posição {pos}')
#print(sorted(lanche)) #ordem alfabetica
a = (2, 5, 4)
b = (5, 8, 1, 2)
c = a + b #Juntará as tuplas
print(c.count(5))#.count contará quantas vezes o mesmo elemento apareceu
                 #len contará quantos elementos tem na tupla
                 #.index mostrara a posição do elemento 
